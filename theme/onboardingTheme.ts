/**
 * Onboarding-specific color palette
 * 
 * This palette is scoped only to the onboarding flow and does not affect
 * other modules (login, food, exercise, etc.). The teal/aqua design is
 * isolated to onboarding screens.
 */
export const onboardingColors = {
  primary: '#16A1B8',
  primaryDark: '#0D7483',
  accent: '#4ED1C5',
  backgroundTint: '#E6FAF8',
} as const;

