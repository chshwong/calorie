# Native app environment variables (apps/native)
#
# Copy to: apps/native/.env
#
# Required for Supabase auth + data
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
#
# Required for wrapped web app (WebView) base URL
# - Must be https:// in production
# - http:// is allowed only in __DEV__ (local development)
EXPO_PUBLIC_WEB_BASE_URL=

