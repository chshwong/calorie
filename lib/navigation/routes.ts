/**
 * Centralized route constants for navigation helpers + shared UI.
 *
 * Note: In Expo Router this is the signed-in "home" (Food Log index tab),
 * not the root "/" route (which runs StartupGate).
 */
export const HOME_ROUTE = '/(tabs)' as const;

